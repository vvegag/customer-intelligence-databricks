# Requirements para Customer Intelligence Project
# Databricks Runtime já inclui a maioria, mas listando para referência.
# Tetos de versão (upper bound) evitam quebra silenciosa em major release —
# antes só havia piso (>=), sem lockfile, o que é risco real de reprodutibilidade
# (ex: numpy 2.x já quebrou compatibilidade do SHAP nesse projeto, ver
# Model_Explainability_SHAP.py). Tetos escolhidos pela major version atual + 1,
# não são valores arbitrários.

# Core ML/Data Science
pandas>=1.5.0,<3.0.0
numpy>=1.23.0,<3.0.0
scikit-learn>=1.2.0,<2.0.0
xgboost>=1.7.0,<3.0.0
lightgbm>=3.3.0,<5.0.0

# Forecasting (04_models/Forecast GMV e Resgates.py)
prophet>=1.1.0,<2.0.0

# Databricks & Spark
databricks-sdk>=0.12.0,<1.0.0
pyspark>=3.4.0,<4.0.0

# MLflow (projeto já roda tanto com 2.x quanto 3.x no runtime do Databricks,
# ver mensagens de erro reais capturadas contra mlflow 3.8.1 nesta sessão)
mlflow>=2.8.0,<4.0.0

# Utilities
python-dateutil>=2.8.0,<3.0.0
pytz>=2023.0

# Visualization (opcional - Databricks tem built-in)
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
plotly>=5.17.0,<7.0.0

# Statistical analysis
scipy>=1.10.0,<2.0.0
statsmodels>=0.14.0,<1.0.0

# Causal inference (para A/B testing)
dowhy>=0.9.0,<1.0.0  # opcional, se usar causal inference avançado
